# Reduce Docker build context & avoid leaking local files
# Note: Do NOT ignore meta/ because runtime stage copies it into the image.
#       Keep public/ included too (often mounted as a volume; harmless in context).

# Rust build artifacts
/target/
**/*.rs.bk

# VCS & CI
/.git/
/.gitignore
/.github/

# Editors & OS junk
/.vscode/
/.idea/
.DS_Store
Thumbs.db

# Logs & temp files
*.log
*.tmp
*.swp
*.swo

# Project docs & tests (not required for building the binary)
/docs/
/test/

# Docker orchestration files (not needed inside image context)
/docker-compose.yml  # Docker orchestration files (not needed inside image context)
/docker-compose.yaml # Docker orchestration files (not needed inside image context)
target/

# VCS / CI
.git/
.github/

# OS/editor junk
**/.DS_Store
.vscode/
.idea/
*.iml

# Logs
**/*.log

# Local content is usually mounted at runtime
# Keep public/ out of build context by default (can be large)
public/

# IMPORTANT: include meta/ so the image has default templates baked in
# (compose still mounts ./meta to override at runtime)
!meta/

# Other
Dockerfile
